/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    unoptimized: true,
  },
  // i18n: {
  //   locales: ["en", "ja"],
  //   defaultLocale: "en",
  // },
};

export default nextConfig;
